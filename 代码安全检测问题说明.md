# 代码安全检测问题说明

本文档用于说明最新代码安全检测报告中的剩余问题，以及本系统当前采取的控制措施。  

## 一、总体结论

本系统已完成主要安全整改：

- 依赖漏洞扫描：前端、后端 `npm audit` 均为 `0`。
- 登录接口已具备失败次数限制。
- JWT 不再只使用用户名作为身份标识，已加入用户 ID 和 `tokenVersion`。
- 用户修改密码后，旧 token 会自动失效。
- SSO 登录不把 JWT 暴露在 URL 中，改用一次性 `ticket` 交换登录态。
- SSO `state` 已校验，用于防止伪造回调。
- 文件上传仅允许 PDF，并进行文件头校验。
- 附件下载路径已限制在上传目录内，防止路径遍历。
- 打包脚本已排除 `.env`、数据库文件、私钥、`node_modules`、构建产物等敏感内容。
- 后端服务默认只监听 `127.0.0.1`，正式访问通过 Nginx 反向代理。

因此，报告中剩余问题不影响本系统按当前部署方式上线联调。

## 二、重点问题说明

| 检测项 | 说明 | 当前处理 |
| --- | --- | --- |
| 登录接口缺少防暴力破解保护 | 扫描器只检查 controller，未识别 service 层逻辑。 | 已在 `AuthService` 中限制同一账号 1 分钟内最多 10 次失败，第 11 次返回 `429`。 |
| SSO 同步接口缺少权限校验 | `syncFromSso` 是内部 service 方法，不是公开 HTTP 接口。外部无法直接调用。 | 只有 `/api/v1/sso/callback` 在完成学校授权码校验和 `state` 校验后才会调用。 |
| `/sso/ticket` 缺少 CSRF 防护 | 本系统未使用 Cookie 自动携带登录态，接口依赖一次性 ticket。传统 Cookie CSRF 风险不适用。 | ticket 有效期 60 秒、一次性使用、使用后立即删除。正式环境要求 HTTPS。 |
| SSO `access_token` 放在 URL 参数 | 校方现有资料给出的用户信息接口方式为 `profile?access_token=xxx`。 | 为兼容校方接口暂保留；该请求发生在后端到学校统一认证服务器之间，不经过浏览器地址栏。正式环境应避免在代理日志中记录完整查询串。 |
| 前端 token 存储在 localStorage | 这是前后端分离系统常见方案，但安全性低于 HttpOnly Cookie。 | 当前不切换架构，避免影响现场 SSO 联调；已缩短暴露面：本地只保存最小用户信息，禁止使用 `v-html`，后端 token 有过期时间，改密后旧 token 失效。 |
| TypeScript `strict: false` | 开启 strict 会引发较大范围类型重构。 | 本轮不作为上线前阻塞项，后续可作为代码质量专项优化。 |
| JWT 有效期 12 小时 | 扫描器建议更短周期加 Refresh Token。 | 当前系统部署在内网业务场景，12 小时用于覆盖一天办公使用；同时改密会让旧 token 失效。后续如学校要求，可改为短 token + refresh 机制。 |
| SSO ticket 存在内存 Map | 扫描器按分布式部署风险提示。 | 当前部署为单实例后端，内存 ticket 可满足要求；如后续多实例部署，再改为 Redis。 |
| package-lock 版本、依赖锁提示 | 项目已提交 `package-lock.json`。部分提示属于工具对 lockfile 的泛化检查。 | 实际安装以 lockfile 为准，`npm audit` 为 0。 |
| HTTPS / HTTP 安全头 | 部署脚本默认生成 HTTP Nginx 配置，HTTPS 通常由现场域名证书决定。 | 正式环境建议由学校或部署方配置 HTTPS 证书、安全响应头和反向代理策略。 |

## 三、SSO 对接安全说明

本系统 SSO 流程如下：

```text
用户点击学校后台菜单
→ 访问 /api/v1/sso/login
→ 跳转学校统一认证
→ 学校回调 /api/v1/sso/callback?code=xxx&state=xxx
→ 后端校验 state
→ 后端用 code 换 access_token
→ 后端获取学校用户信息
→ 同步本地用户
→ 生成一次性 ticket
→ 前端用 ticket 换取本系统 JWT
```

安全控制点：

- `client_secret` 只在后端配置，不进入前端。
- `state` 有效期 10 分钟，用于防止伪造 SSO 回调。
- JWT 不拼接在 URL 中。
- 一次性 ticket 有效期 60 秒，使用后删除。
- SSO 自动绑定本地管理员账号已被限制，避免普通 SSO 账号绑定管理员账号。
- SSO 新用户默认按学校 `roleUuid` 映射；如未配置管理员角色 UUID，则默认普通用户。

## 四、登录与权限安全说明

本系统登录和权限控制采用：

- JWT Bearer Token。
- 后端 `AuthGuard` 校验 token。
- 后端 `RolesGuard` 校验角色。
- 前端路由仅作为用户体验控制，不作为最终安全边界。
- 所有关键接口均以后端角色校验为准。

已采取措施：

- JWT payload 使用 `user.id`，不是用户名。
- JWT 包含 `tokenVersion`。
- 修改密码后 `tokenVersion` 增加，旧 token 自动失效。
- 停用账号无法继续登录。
- 用户管理接口仅超级管理员可访问。
- 创建用户时默认普通用户，不能由客户端直接创建管理员。
- 管理员角色变更必须走超级管理员接口。

## 五、文件上传下载安全说明

项目验收材料上传和下载已做以下控制：

- 仅允许上传 `.pdf`。
- 限制单文件大小。
- 后端校验 PDF 文件头。
- 文件保存名使用随机 UUID，避免覆盖和路径注入。
- 下载前进行业务权限校验。
- 下载路径通过 `realpath` 限制在上传目录内。
- 下载文件名进行清洗，避免异常字符进入响应头。

## 六、部署要求

为了让上述安全控制生效，正式部署需满足：

- 使用交付包 `release/*.zip` 进行部署和检测，不直接上传开发工作目录。
- 后端 `.env` 不进入交付包。
- 数据库 `.db` 不进入交付包。
- 私钥、证书、账号密码不进入代码仓库和交付包。
- 正式环境建议使用 HTTPS。
- `JWT_SECRET` 使用强随机字符串。
- `SSO_CLIENT_SECRET` 只配置在后端服务器。
- 后端服务通过 Nginx 代理访问，不直接暴露后端端口。

## 七、仍可后续增强的事项

以下事项属于增强项，不影响当前版本上线联调：

- 将登录态从 localStorage JWT 改为 HttpOnly Cookie。
- 将 JWT 改为短有效期 token + refresh token。
- SSO ticket 存储由内存 Map 改为 Redis，以支持多实例部署。
- 开启 TypeScript strict 并做类型重构。
- 增加统一安全响应头，如 HSTS、X-Frame-Options、Content-Security-Policy。
- 增加登录审计日志和异常登录告警。

