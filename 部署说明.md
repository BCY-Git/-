# 供应商轮候抽取系统 NestJS 版部署说明

本文档用于在内网服务器部署 NestJS 版本。系统部署后通过局域网浏览器访问。

## 一、部署架构

- 前端：Vue 构建后的静态文件，由 Nginx 提供访问。
- 后端：NestJS，监听本机 `8000` 端口。
- 反向代理：Nginx 监听 `80` 端口，并把 `/api/` 请求转发到后端。
- 数据库：SQLite 文件数据库，由 Prisma 初始化和维护。
- 文件上传目录：服务器本地目录。
- 服务管理：systemd。

默认访问地址：

```text
http://服务器局域网IP/
```

## 二、服务器要求

推荐环境：

- 操作系统：CentOS 7 或同类 Linux 服务器。
- CPU / 内存：2 核 4G 及以上。
- 磁盘：建议 20G 以上可用空间。
- Node.js：`>= 18.18.0`。
- npm。
- nginx。
- systemd。

CentOS 7 自带 glibc 较老，直接安装新版 Node.js 可能不兼容。若甲方服务器确认为 CentOS 7，建议使用其单位认可的 Node.js 18.18+ 安装方式，或使用已适配 CentOS 7 的 Node.js 离线包。

## 三、本地验证

后端：

```bash
cd backend
npm install
cp .env.example .env
npm run db:init
npm run dev
```

前端：

```bash
cd frontend
npm install
npm run dev
```

访问：

```text
http://127.0.0.1:5173
```

## 四、一键部署

在项目根目录执行：

```bash
sudo bash deploy/deploy.sh
```

脚本会自动完成：

- 创建部署目录。
- 复制项目文件到 `/opt/lingxuan-nest`。
- 创建环境变量文件 `/etc/lingxuan-nest/backend.env`。
- 安装后端依赖并初始化 Prisma SQLite 数据库。
- 构建 NestJS 后端。
- 安装前端依赖并构建静态文件。
- 发布前端到 `/var/www/lingxuan-nest`。
- 注册并启动后端 systemd 服务。
- 配置并重启 nginx。
- 尝试开放防火墙 `80/tcp`。

## 五、初始账号

系统初始化时只创建超级管理员 `admin`。初始密码由部署脚本随机生成，首次创建环境变量文件时会在终端输出，同时写入 `/etc/lingxuan-nest/backend.env` 的 `INIT_ADMIN_PASSWORD`。

正式使用前，请登录系统后立即修改初始密码，并由超级管理员在“用户管理”中创建管理员和普通用户账号。

## 六、常用运维命令

查看后端服务状态：

```bash
systemctl status lingxuan-nest-backend
```

重启后端：

```bash
systemctl restart lingxuan-nest-backend
```

查看后端日志：

```bash
journalctl -u lingxuan-nest-backend -f
```

重启 Nginx：

```bash
systemctl restart nginx
```

检查 Nginx 配置：

```bash
nginx -t
```

## 七、部署目录

| 路径 | 用途 |
| --- | --- |
| `/opt/lingxuan-nest` | 项目源码与构建产物 |
| `/var/www/lingxuan-nest` | 前端静态文件 |
| `/var/lib/lingxuan-nest` | SQLite 数据库和上传文件 |
| `/etc/lingxuan-nest/backend.env` | 后端环境变量 |
| `/etc/nginx/conf.d/lingxuan-nest.conf` | Nginx 配置 |

用户统一认证/单点登录属于后续现场对接项；当前版本保留本地账号体系用于部署验证和演示。
