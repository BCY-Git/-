DATABASE_URL="file:./supplier_system.db"
JWT_SECRET="请用 openssl rand -hex 32 生成"
INIT_ADMIN_PASSWORD="请配置强初始密码"
CORS_ORIGIN=

# Optional SMTP settings for later integration
SMTP_HOST=
SMTP_PORT=25
SMTP_FROM=
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_TLS=false
